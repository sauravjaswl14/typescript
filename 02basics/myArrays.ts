const people: string[] = ['anna', 'ryu', 'morgan'];

const numbers: Array<number> = [23, 43, 23, 12, 65];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({ name: 'john', isActive: false });

const MLmodel: number[][] = [
  [255, 255, 255],
  [90, 180, 230],
];
